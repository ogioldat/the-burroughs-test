name: Tests - CI/CD
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: test

    steps:
      - name: Check out repository code
        uses: actions/checkout@v2

      - name: Run tests
        run: yarn test

